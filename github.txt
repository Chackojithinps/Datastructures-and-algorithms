.All the history that we are making is get stored in an another folder that git provides(that is known as git repository)
 it is .git(folder)
.git init - initializating an empty git repository.repository means a folder.